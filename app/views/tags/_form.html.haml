=form_with(url: include_story_type_tags_path(@story_type),
           method: :put, html: { onchange: '$(this).submit();' }) do |form|
  =form.collection_select :tag_id, Tag.all, :id, :name, { prompt: '' }, { name: :id }
