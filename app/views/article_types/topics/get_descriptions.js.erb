$('select#article_type_topic_id').empty();
$('select#article_type_topic_id').append( $('<option>Select the Description</option>'));
<% @topics.each do |topic| %>
  $('select#article_type_topic_id').append($('<option value="<%= topic.id %>"><%= topic.description %></option>'));
<% end %>
