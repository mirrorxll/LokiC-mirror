.small
  -iteration = @story_type.iteration
  -if @story_type.staging_table && iteration.creation && iteration.schedule
    -samples = iteration.samples
    -exported = samples.where.not(pl_production_id: nil).count
    -total = samples.count

    -if iteration.export
      .row
        .col-11.offset-1
          total exported: #{total}
    -else
      .row
        .col-1.offset-1
          =link_to('export!', production_story_type_iteration_exports_path(@story_type, @story_type.iteration),
                              class: 'btn btn-sm btn-outline-dark',
                              method: :post, remote: true)
        .col-9.small
          exported/total:
          ="#{exported}/#{total}"
