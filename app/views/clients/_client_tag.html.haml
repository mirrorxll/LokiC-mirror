%tr{id: "client_#{client_tag.client.id}_tag"}
  %td.client
    =render client_tag.client
  %td.client_tag
    -if client_tag.tag
      =render client_tag.tag, client: client_tag.client
    -else
      =render 'tags/form', client: client_tag.client
