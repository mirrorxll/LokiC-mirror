= form_with(url: include_story_clients_path(@story)) do |form|
  = form.label :clients
  = form.collection_select :client_id, Client.all, :id, :name, { prompt: true }, { name: :id }
  = form.submit 'Add', data: { disable_with: "..." }