.row
  .col-12
    =link_to('[ reload ]', reload_factoid_type_codes_path(@factoid_type),
             class: 'p-0 btn btn-sm btn-link', method: :put, data: { disable_with: '[ ... ]'}, remote: true)
    =link_to('[ download ]', rails_blob_path(@factoid_type.code), class: 'p-0 btn btn-sm btn-link')
    =link_to('[ show ]', factoid_type_codes_path(@factoid_type), class: 'p-0 btn btn-sm btn-link', remote: true)
