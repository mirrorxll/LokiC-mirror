-if @iteration.creation && !@iteration.purge_creation.eql?(true)
  -articles = @iteration.articles
  -published = articles.published.count
  -ready_to_publish = articles.not_published.count

  #factoid_type_export.card.h-100
    .card-header.d-flex.justify-content-between.align-items-center
      EXPORT (iteration: #{@iteration.name})
      #export_control
        =render 'factoid_types/exports/export_control'

    #export
      .flash
      .card-body.d-flex.flex-column.text-center
        =render 'factoid_types/publishes/publish', articles: articles,
                                                   published: published,
                                                   ready_to_publish: ready_to_publish
