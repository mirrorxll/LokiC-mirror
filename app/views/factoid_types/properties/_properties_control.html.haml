-#TODO: if this is correct implementation of user permissions?
-if current_account_permissions('factoid_types')['edit']
  -# =button_to('Edit', factoid_type_properties_form_path(@factoid_type), method: :get, remote: true, disabled: !blocked_item?,
  =button_to('Edit', factoid_type_properties_form_path(@factoid_type), method: :get, remote: true, disabled: !factoid_types_blocked_item?,
               data: { toggle: 'modal', target: '#factoidTypePropertiesForm' })