.row.small
  .col-10.offset-1
    =form_with(url: story_type_codes_path(@story_type)) do |form|
      =form.submit 'attach code from DB', class: 'btn btn-sm btn-outline-dark', data: { disable_with: '...'}
