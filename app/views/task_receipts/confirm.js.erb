<%if current_account.eql?(@task_receipt.task.creator) || current_account.types.include?('manager') %>
$('#confirms').html('<%= j render 'task_receipts/confirms', task_receipts: @task_receipts %>')
<%else %>
$('#confirms').html('<%= j render 'task_receipts/confirmed' %>')
<%end %>
