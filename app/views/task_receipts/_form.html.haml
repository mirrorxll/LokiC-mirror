=form_with(url: confirm_task_receipts_path(@task), method: :patch, html: { onchange: "Rails.fire(this, 'submit')" }) do |form|
  .form-check.mt-2.ml-2.align-items-center
    =form.check_box :confirmed, class: 'form-check-input'
    %label.form-check-label
      I have read the task, and I understand what I need to do.
