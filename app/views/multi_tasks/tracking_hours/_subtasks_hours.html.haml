-task.subtasks.each do |subtask|
  .row.font-weight-bold.small.col
    =title(subtask.title)
  .row.small.col-10
    -subtask.assignments.each do |assignment|
      .col-6
        =assignment.name
      .col-6
        =assignment.hours.nil? ? '---' : sprintf('%g', assignment.hours.to_s) + ' hours'
  %hr
