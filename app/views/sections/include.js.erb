$('#sections').append(`
  <div id="section-<%= @section.id %>">
    <%= @section.name %>
    <%= link_to('X', exclude_story_section_path(story_id: @story.id, id: @section.id), method: :delete, remote: true) %>
  </div>
`)
