-if task.assignment_to.empty?
  .font-weight-bold.small
    assignment to:
-else
  -task.assignment_to.each do |assignment|
    .badge.badge-info
      =assignment.first_name + ' ' + assignment.last_name

=link_to '[ edit ]', edit_task_assignments_path(task), remote: true, class: 'small'
