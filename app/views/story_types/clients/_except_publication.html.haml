.row.mb-1{ id: "excepted_publication_#{excepted_publication.id}" }
  .col-4.client
    =excepted_publication.client.name
  .col-4.publication
    =publication.name
    =link_to('x', exclude_story_type_client_publication_path(@story_type, client_publication_tag.client, publication, client_publication_tag: client_publication_tag), method: :delete, remote: true)
