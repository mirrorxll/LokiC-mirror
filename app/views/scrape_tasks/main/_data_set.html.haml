.row.mt-2
  .col-3.pr-0.text-right.font-weight-bold
    HLE data set:
  .col-9
    -if @data_set&.persisted?
      =link_to(@data_set.name, @data_set)
    -else
      =link_to('[ create ]', '#', "data-toggle" => "modal", "data-target" => "#dataSetForm")
