=render 'links'

%section#filter.mt-3
  =render 'filter_scores'

%hr/
%section#developers_production.small
  .row.text-center.justify-content-center
    .col-2 developer
    .col-2 hours
    .col-2 score
  %hr/
  -@rows_reports.each do |row|
    -puts row[:developer].first_name
    %hr/
    .row.text-center.justify-content-center
      .col-2
        ="#{row[:developer].first_name} #{row[:developer].last_name}"
      .col-2
        =row[:hours]
      .col-2
        =row[:score]


#scores-data.modal.fade.scores-data-modal-lg{"aria-hidden" => "true", "aria-labelledby" => "import-data", role: "dialog", :tabindex => "-1"}
  .modal-dialog.modal-dialog-centered.modal-lg
    .modal-content
      .d-flex.justify-content-end.mt-1.mr-1
        %button.close{"aria-label" => "Close", "data-dismiss" => "modal", type: "button"}
          %span{"aria-hidden" => "true"} ×
      .modal-body

