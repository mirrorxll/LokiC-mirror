%p
  Table Name:
  = @story_type.staging_table.name
- @story_type.reload.staging_table.columns.each do |column|
  %p
    = "#{column['name']} : #{column['type']}"
%p
  = link_to('edit',
      edit_story_staging_table_path(story_type_id: @story_type.id, id: @story_type.staging_table.id),
      class: 'btn btn-sm btn-success',
      remote: true)
