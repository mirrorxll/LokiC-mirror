%p.mb-1
  creation
.row
  #create.col-2
    = link_to('create', execute_story_populations_path(@story_type), method: :post)
  #create-purge.col-10
    = link_to('purge last creation iteration', purge_story_populations_path(@story_type), method: :delete)
