%p.mb-1
  creation
.row
  #create.col-2
    = link_to('create', story_type_populations_path(@story_type), method: :post)
  #create-purge.col-10
    = link_to('purge last creation iteration', story_type_populations_path(@story_type), method: :delete)
