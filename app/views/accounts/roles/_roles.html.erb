<div id="roles" class="row small">
  <div class="col-sm-2 col-lg-1 text-right">
    <span class="font-weight-bold">ROLES:</span>
    <br>
    <%= link_to('[ edit ]', edit_account_roles_path(@account), class: 'btn btn-sm btn-link p-0', remote: true) %>
  </div>

  <div class="col pl-0">
    <% @account.roles.each do |r| %>
      <%= r.name %>
      <br>
    <% end %>
  </div>
</div>
