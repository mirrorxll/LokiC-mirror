$('#editAccessLevel<%= @card.id %> .access-level>.row>:first-child>a').addClass('disabled');

$('#editAccessLevel<%= @card.id %> .modal-body')
    .html('<%= j render 'accounts/cards/access_levels/form', { account: @account, card: @card, template: @access_level_template } %>');
