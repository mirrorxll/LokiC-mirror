=form_with(url: include_client_data_set_default_properties_path(@data_set), html: { onchange: "Rails.fire(this, 'submit')" }) do |form|
  =form.collection_select :client_id, Client.where(hidden: false).order(name: :asc), :id, :name, { prompt: '' }, { name: :id }
