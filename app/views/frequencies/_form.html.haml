= form_with(url: include_story_frequencies_path(@story), remote: true) do |form|
  = form.label :frequency
  = form.collection_select :frequency_id, Frequency.all, :id, :name, { prompt: true }, { name: :id }
  = form.submit 'Add'
