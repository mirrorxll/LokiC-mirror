=form_with(model: @data_set) do |form|
  -data_set_keys.each do |field|
    .row
      .col-3.text-right
        =form.label field
      .col-9
        -if %i[cron_scraping evaluated].include?(field)
          =form.check_box field
        -else
          =form.text_field field
  .row
    .col-12
      =form.submit  data: { disable_with: "..." }
